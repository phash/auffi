node_modules/
dist/
build/
target/
*.log
.env
.env.local
.env.prod
.DS_Store
.vite/
src-tauri/target/
coverage/
.vitest-cache/
sharer/src-tauri/gen/
ops/.env.deploy

# Local act secrets — never commit credentials
.secrets

# Locally-staged release artefacts (binaries, not source)
release-bin/

# Local screenshots — manuel's personal capture dir
screens/
